# Required only for the online-mode cloud upgrade (Groq free tier).
# Get a free key at https://console.groq.com/keys
# Offline mode (WebLLM + Gemma 2B) needs no key at all — it runs entirely
# in the user's browser.
GROQ_API_KEY=
